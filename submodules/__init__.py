"""Submodules — decoupled from family main modules.

Each submodule depends only on interfaces (bus, capabilities, logging,
permissions, llm) and communicates with main modules via the message bus.
"""
